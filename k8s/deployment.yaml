apiVersion: apps/v1
kind: Deployment
metadata:
  name: form-app-deployment
  labels:
    app: form-app
spec:
  replicas: 2  # Adjust to desired number of replicas
  selector:
    matchLabels:
      app: form-app
  template:
    metadata:
      labels:
        app: form-app
    spec:
      containers:
        - name: form-app
          image: <containers:
  - name: form-app
    image: <ECR_REPO_URL>:latest  # Replace with actual image name
          ports:
            - containerPort: 8080
